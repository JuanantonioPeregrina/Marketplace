<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= anuncio.titulo %></title>
</head>
<body>
    <h1><%= anuncio.titulo %></h1>
    <img src="<%= anuncio.imagen %>" alt="Imagen del anuncio" style="max-width: 300px;">
    <p><%= anuncio.descripcion %></p>
    <p>📍 Ubicación: <%= anuncio.ubicacion %></p>
    <p>💰 Precio Actual: <strong><%= anuncio.precioActual %>€</strong></p>
    <p>🕒 Publicado por: <%= anuncio.autor %></p>

    <h3>Pujas realizadas:</h3>
    <ul>
        <% if (anuncio.pujas.length > 0) { %>
            <% anuncio.pujas.forEach(puja => { %>
                <li><strong><%= puja.usuario %>:</strong> <%= puja.cantidad %>€</li>
            <% }) %>
        <% } else { %>
            <p>Aún no hay pujas.</p>
        <% } %>
    </ul>

    <h3>Hacer una puja</h3>
    <% if (user) { %>
        <form action="/api/anuncios/<%= anuncio._id %>/pujar" method="POST">
            <input type="number" name="cantidad" required>
            <button type="submit">Pujar</button>
        </form>
    <% } else { %>
        <p>Debes iniciar sesión para pujar.</p>
    <% } %>

    <a href="/anuncios">🔙 Volver a la lista de anuncios</a>
</body>
</html>
