<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/styles.css">
</head>

<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">
    <!-- Header -->
    <%- include("partials/header") %>

    <!-- Contenido principal -->
    <main class="max-w-7xl mx-auto pt-24 px-6">
        <% if (categoriaNombre && descripcion && imagen) { %>
            <!-- Vista de Categoría Específica -->
            <h1 class="text-4xl font-bold text-center mb-6"><%= categoriaNombre %></h1>
            <p class="text-center text-gray-700 mb-6"><%= descripcion %></p>

            <div class="relative w-full h-[300px] overflow-hidden rounded-lg shadow-lg">
                <img src="<%= imagen %>" alt="<%= categoriaNombre %>" class="w-full h-full object-cover">
            </div>
        <% } else { %>
            <!-- Vista de Lista de Categorías -->
            <h1 class="text-4xl font-bold text-center mb-6">Explora Nuestras Categorías</h1>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
                <% for (let key in categorias) { %>
                    <a href="/categorias/<%= key %>" class="block">
                        <div class="relative w-full h-[250px] overflow-hidden rounded-lg shadow-lg hover:scale-105 transition-transform">
                            <img src="<%= categorias[key].imagen %>" alt="<%= categorias[key].nombre %>" class="w-full h-full object-cover">
                            <div class="absolute bottom-3 left-3 bg-black/80 text-white px-3 py-1 rounded-md text-sm">
                                <%= categorias[key].nombre %>
                            </div>
                        </div>
                    </a>
                <% } %>
            </div>
        <% } %>
    </main>

    <!-- Footer -->
    <%- include("partials/footer") %>
</body>
</html>
